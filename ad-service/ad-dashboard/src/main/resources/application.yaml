server:
  port: 7002

spring:
  application:
    name: ad-dashboard

eureka:
  client:
    service-url:
      defaultZone: http://server1:8000/eureka

# 开启服务监控功能 且暴露所有监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"